# Marvel Watchlist Web

SvelteKit adapter-static frontend for the Marvel Watchlist.

## Commands

```sh
npm install
npm run dev
npm run build
npm run check
npm run test
```

The production output is `build/`. Azure Static Web Apps deploys this app with the managed API from `../api`.
